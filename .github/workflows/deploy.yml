name: Deploy to GitHub Pages

on:
  push:
    branches: [main] 
  2. ⚠️ Node.js 20 deprecated - cần upgrade lên Node.js 24

---

## 🔧 **Cách sửa:**

Cập nhật file `.

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'  # ← Thay từ '18' thành '20'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      github/workflows/deploy.yml` - thay `node-version: '18'` thành `'20'` hoặc `'lts'`:

```yaml name=.github/workflows/deploy.yml
- uses: actions/setup-node@v4
  with:
    node-version: '20'  
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
